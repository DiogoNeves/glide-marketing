# Codex Adapter

Use when installing Glide into a Codex workspace.

- root instructions: `AGENTS.md`
- skills: `.agents/skills/<skill-name>/SKILL.md`
- workspace: `Glide HQ/`

Use `ROOT_AGENTS_SECTION.md` for the root instruction snippet.
